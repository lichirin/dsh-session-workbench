# dsh-session-workbench bundle: mount the plugin row. The client half loads
# automatically via exports["./client"] + dsh.client.platform = web.
- insert:
    - id: session-workbench
      name: 'dsh-session-workbench'
